# AGENTS

Lightweight personal workspace. Prefer answering the user over process meta-commentary.

- Durable host/layout facts live in `INFRA.md` / the `USER.md` index — not long essays in chat.
- Do not copy secrets into workspace files.
- When infra changes, refresh the short index only.
